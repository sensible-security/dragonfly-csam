// Browser entry point for the Vite client bundle.
// Global stylesheets (Beer CSS, static/styles.css) are linked directly in
// routes/_app.tsx from static/ per docs/beercss-conventions.md §1, so nothing
// is imported here yet.
