import { define } from "../utils.ts";

export default define.page(function AuditLogPage() {
  return (
    <article class="border round">
      <h5>Audit Log</h5>
      <p>Audit log viewer (CIS Control 8 groundwork) arrives in Phase 4.</p>
    </article>
  );
});
