import { define } from "../utils.ts";

export default define.page(function DevicesPage() {
  return (
    <article class="border round">
      <h5>Devices</h5>
      <p>Device inventory (CIS Safeguards 1.1, 1.2) arrives in Phase 4.</p>
    </article>
  );
});
