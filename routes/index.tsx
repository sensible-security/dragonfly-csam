import { define } from "../utils.ts";

export default define.page(function DashboardPage() {
  return (
    <article class="border round">
      <h5>Dashboard</h5>
      <p>
        Inventory KPI cards arrive in Phase 4. Dragonfly CSAM implements CIS
        Controls v8.1, Controls 1 and 2: you cannot defend what you don't know
        you have.
      </p>
    </article>
  );
});
