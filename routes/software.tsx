import { define } from "../utils.ts";

export default define.page(function SoftwarePage() {
  return (
    <article class="border round">
      <h5>Software</h5>
      <p>Software inventory (CIS Safeguards 2.1–2.3) arrives in Phase 4.</p>
    </article>
  );
});
